services:
  - type: web
    name: yt-comment-finder
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: YOUTUBE_API_KEY
        sync: false          # set manually in Render dashboard — never committed to git
